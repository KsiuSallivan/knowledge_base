def count_up(n):
    # Базовый случай: если n меньше или равно 0, просто возвращаем
    if n <= 0:
        return
    # Рекурсивный случай: сначала вызываем функцию с n-1
    count_up(n - 1)
    # Затем выводим текущее значение n
    print(n)


# Пример использования
count_up(5)